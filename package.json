{
  "name": "mentionloom",
  "version": "0.1.0",
  "private": true,
  "description": "Mentionloom AI discovery landing page",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test checks/waitlist.test.mjs checks/waitlist-api.test.mjs checks/dashboard.test.mjs",
    "waitlist:export": "node --env-file=.env.local scripts/export-waitlist.mjs"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
